<div class="testbox">
  <div class="tickettitle">
    <div class="whitetitle">
      <strong><p class="XLfont lopadtop">Comments</p></strong>
      <p class="Mfont book lopadbot"><%= link_to "New", new_command_comment_path(ticket), class: "nolink" %></p>
    </div>
  </div>
  <% if ticket.comments.length > 0 %>
    <%= render ticket.comments %>
  <% end %>
</div>
